apiVersion: v1
kind: Service
metadata:
  name: devlake-service
  namespace: argocd
spec:
  selector:
    app: devlake
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
